node_modules/
dist/
*.log
.env
.env.*
.DS_Store
coverage/
.vitest-cache/

# cortex runtime files — never commit these
~/.cortex/
config.json
api-key.enc
manifest.json
path-mappings.json

# dev-only context for Claude Code — kept local, not published in this open-source repo
/CLAUDE.md
